1. ABC в буквах (Витя) или минимальный размер в диалоге (Максим)
2. Перемещение картинки (Витя)
3. Масштабирование (Максим)
4. Tab (Максим)
5. Выделение слов (Лёша)

	Проблемы: 
0. Не копируется картинка, когда она попадаёт в интревал копирования.
Причина: копируем только буквы, в буфере обмена только unicode - строка, 
придётся искусственно отслеживать попала ли каринка в простанство выделения.
Либо забить на сей баг.
1. Прокоментированный кусок из  конструктора ContentOfWindow
	//раньше не было проблем с наползанием букв, ибо использовали
	//фиксированный шрифт, возвращаемся к этой пркатике
	//this->currentFont = (HFONT) GetStockObject(DEFAULT_GUI_FONT);
	this->currentFont = (HFONT) GetStockObject(SYSTEM_FIXED_FONT);
	//но сие забивает на проблему с ABC, не решая её
	//пока сгодится, если всё остальное будет работать, то будем разбираться
	//с ABC
	//LOGFONT lf; //понадобится, если будем проблему с ABC всё-таки решать
	hDC = GetDC(hWnd);
	//SelectObject(hDC, GetStockObject(SYSTEM_FIXED_FONT));
	GetClientRect(hWnd, &clientRect);
	
	Сей кусок ощищен от комментов в исходниках, чтобы Пинюта случайно не запалил
	Суть: когда работали с ячейками по размерам букв, то у нас начальным был не DEFAULT_GUI_FONT,
у которого ширина не фиксированная, а SYSTEM_FIXED_FONT у которого фиксированная.
Насчёт этого (причины начатия порнухи с наползанием букв друг на друга) заикался Макс, но
я сказал ему, что искать причину это херня. Как видим, если бы тогда поискали бы, то не мучались
бы с исправлением наползания в начальном шрифте, по сему я - самодовольный самодур,
считающий себя умнее прочих, а Макс - молодец, ибо задавал верный вопрос.
	Проблема с выбором шрифтов в диалоге меньше 14 решена, но криво. Если не хотим переписывать
getLinesInfo(), indexByCaret(int index), pixelLowerCornerByIndex(int index) 
и printCharOnDC(CharInfo symbol, POINT lowLeftAngle, int indexLine), то придётся оставить
с такими заплатами, когда при выборе шрифта не из диапазона 14 - 72 вылазит окошко.
2. Какае-то странность с удалением после измения шрифта, удаялет как если бы выделение было
не снято.
3. При открытии трёх картинок, какае-то странная херня, как у Лёши было: текст под картинку
уходит. Возможно это из-за того, что не помешается оно без полос прокрутки.
4. Захват картинки в область измения шрифта приведёт к страшной порнухе.
5. Во время выделение каретки не видно.